
# Build

```bash
latexmk -pdf -interaction=nonstopmode -halt-on-error reverse_hessian_status_synthesis.tex
```

The report is a status synthesis, not a replacement for the two autonomous papers or an assertion of `VP != VNP`.
