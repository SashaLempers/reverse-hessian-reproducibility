
# CI

The active GitHub Actions workflow is at `../.github/workflows/verify.yml`, the only repository-root location recognized by GitHub Actions. This directory remains as a compatibility pointer for the v2 layout.
